
#Область КомандыФормы

&НаКлиенте
Процедура СформироватьОчередьВыполненияСлужб(команда)
	
	СформироватьОчередьВыполненияСлужбНаСервере();
	
	ОповеститьОбИзменении(Тип("РегистрСведенийКлючЗаписи.ОчередьВыполненияСлужб"));

КонецПроцедуры

&НаКлиенте
Процедура ОчиститьОчередь(команда)
	
	ОчиститьОчередьНаСервере();
	
	ОповеститьОбИзменении(Тип("РегистрСведенийКлючЗаписи.ОчередьВыполненияСлужб"));
	
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьОчередь(Команда)
	
	ВыполнитьОчередьНаСервере();
	
	ОповеститьОбИзменении(Тип("РегистрСведенийКлючЗаписи.ОчередьВыполненияСлужб"));
	
КонецПроцедуры

#КонецОбласти

&НаСервере
Процедура СформироватьОчередьВыполненияСлужбНаСервере()
	РегистрыСведений.ОчередьВыполненияСлужб.СформироватьОчередь();
КонецПроцедуры

&НаСервере
Процедура ОчиститьОчередьНаСервере()
	РегистрыСведений.ОчередьВыполненияСлужб.ОчиститьОчередь();
КонецПроцедуры


&НаСервере
Процедура ВыполнитьОчередьНаСервере()
	РегистрыСведений.ОчередьВыполненияСлужб.ВыполнитьОчередь();
КонецПроцедуры


