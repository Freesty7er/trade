
#Область ОбработчикиСобытий

Процедура ОбработкаПроведения(отказ, режимПроведения)
	
	структураДвижений = ХранилищеДвиженийПоРегистрам.Получить();
	
	Для Каждого запись Из структураДвижений Цикл
		Движения[запись.Ключ].Записывать = Истина;
		Движения[запись.Ключ].Загрузить(запись.Значение);
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти
