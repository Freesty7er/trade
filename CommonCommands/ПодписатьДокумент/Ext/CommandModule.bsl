
&НаКлиенте
Процедура ОбработкаКоманды(параметрКоманды, параметрыВыполненияКоманды)
	
	Состояние("Выполняется отметка о проверке бухгалтером");
	
	ПользователиПривилегированный.ПодписатьДокументы(параметрКоманды);	
	
	ОповеститьОбИзменении(ТипЗнч(параметрКоманды[0]));
	
	Состояние();
	
КонецПроцедуры
