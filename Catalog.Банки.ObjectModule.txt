
#Область ОбработчикиСобытий

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	Если ЭтоГруппа Тогда
		ПроверяемыеРеквизиты.Удалить(ПроверяемыеРеквизиты.Найти("Код"));
	Иначе
		
		Если Не ПроверкаДанныхКлиентСервер.ВСтрокеТолькоЦифры(Код) Тогда
			ПроверкаДанныхКлиентСервер.СообщитьОбОшибке(Отказ, НСтр("ru = 'МФО должен содержать только цифры.'"), ЭтотОбъект, "Код");
		КонецЕсли; 
		Если Не ПроверкаДанныхКлиентСервер.ВСтрокеТолькоЦифры(КорреспондентскийСчёт) Тогда
			ПроверкаДанныхКлиентСервер.СообщитьОбОшибке(Отказ, НСтр("ru = 'Корреспондентский счёт должен содержать только цифры.'"), ЭтотОбъект, "КорреспондентскийСчёт");
		КонецЕсли; 
		
	КонецЕсли; 
	
КонецПроцедуры

Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		// Обмен данными. Проверки не выполняем.
		Возврат;
	КонецЕсли; 
	
	Если Не ПометкаУдаления Тогда
		
		Если ЭтоГруппа Тогда
			Код = Неопределено;
		КонецЕсли; 
		
	КонецЕсли; 
	
КонецПроцедуры

#КонецОбласти
