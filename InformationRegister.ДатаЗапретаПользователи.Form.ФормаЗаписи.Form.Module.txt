
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Для к=1 по Метаданные.Документы.Количество() Цикл
		текВид = Метаданные.Документы.Получить(к-1);
		ВыбВидДок.Добавить(к,текВид.имя);

	КонецЦикла
	
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьВидДокумента(Команда)
	Выб=ВыбВидДок.ВыбратьЭлемент("Выберите вид документа");
	Если Выб<> Неопределено тогда
		ЭтотОбъект.Запись.ВидДокумента = СокрЛП(Выб.Представление);
	КонецЕсли;
	
КонецПроцедуры
