// СписокЗначений для накапливания пакета сообщений в журнал регистрации, 
// формируемых в клиентской бизнес-логике.
Перем СообщенияДляЖурналаРегистрации Экспорт; 


// РаботаСФайлами
Перем КомпонентаTwain Экспорт; // Twain компонента для работы со сканером
// Конец РаботаСФайлами

// ФайловыеФункции
Перем ПроверкаДоступаКРабочемуКаталогуВыполнена Экспорт; // Кэшируется, чтобы в данном сеансе повторно не делать проверку доступа к каталогу на диске
// Конец ФайловыеФункции

// Структура, содержащая в себе время начала и окончания обновления программы.
Перем ПараметрыРаботыКлиентаПриОбновлении Экспорт;

// СтандартныеПодсистемы.ОценкаПроизводительности
Перем ОценкаПроизводительностиЗамерВремени Экспорт;

Процедура ПриНачалеРаботыСистемы()
	
	Если Не СоединенияИБКлиент.ПроверитьПользователя() Тогда
		Предупреждение("Пользователь с таким именем уже выполнил вход в систему", 10);
		ЗавершитьРаботуСистемы(Ложь);
		Возврат;
	КонецЕсли;
	
	// инициализируем параметры сеанса в RLS
	Пользователи.ТекущийПользователь();
	Пользователи.ТекущаяОбластьДанных();
	
	Пользователи.ДоступныеКатегорииРаботников();
	Пользователи.ДоступныеПодразделения();
	
	
	Р_ОбщегоНазначения_ПОС.ОбщиеДополнительныеДействияПриНачалеРаботыСистемы();
	
		
	ПользователиОповеститьСервер.ПриНачалеРаботыСистемы();
	
	ПодключитьОбработчикОжидания("ТекущийПользовательЗавершитьРаботуСистемы",36);
	
	
КонецПроцедуры

