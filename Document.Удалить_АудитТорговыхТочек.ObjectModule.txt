
Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	
	Для Каждого СтрокаДокумента Из СтрокиДокумента Цикл
		
		Если НЕ СтрокаДокумента.Свойство_КолвоСКУ = 0 Тогда
			
			СправочникОбъект = СтрокаДокумента.Контрагент.ПолучитьОбъект();
			
			СправочникОбъект.Свойство_КолвоСКУ = СтрокаДокумента.Свойство_КолвоСКУ;
			
			Если СправочникОбъект.Свойство_КолвоСКУ >= 60 Тогда
				СправочникОбъект.КатегорияТорговойТочкий = Перечисления.КатегорияТорговойТочки.A;
			ИначеЕсли СправочникОбъект.Свойство_КолвоСКУ >= 30 Тогда
				СправочникОбъект.КатегорияТорговойТочкий = Перечисления.КатегорияТорговойТочки.B;
			Иначе
				СправочникОбъект.КатегорияТорговойТочкий = Перечисления.КатегорияТорговойТочки.C;
			КонецЕсли;
			
			СправочникОбъект.АдресДоставкиОбласть = СтрокаДокумента.АдресДоставкиОбласть;
			СправочникОбъект.АдресДоставкиРайон = СтрокаДокумента.АдресДоставкиРайон;
			СправочникОбъект.АдресДоставкиГород = СтрокаДокумента.АдресДоставкиГород;
			СправочникОбъект.АдресДоставкиУлица = СтрокаДокумента.АдресДоставкиУлица;
			СправочникОбъект.АдресДоставкиДом = СтрокаДокумента.АдресДоставкиДом;
			СправочникОбъект.АдресДоставкиСтроение = СтрокаДокумента.АдресДоставкиСтроение;
			
			СправочникОбъект.Записать();
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры
