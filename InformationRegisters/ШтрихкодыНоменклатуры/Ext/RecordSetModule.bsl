
Процедура ПередЗаписью(отказ, замещение)
	
	Если Не замещение И Не ОбменДанными.Загрузка Тогда
		
		Для Каждого записьНабора Из ЭтотОбъект Цикл
			
			Если Не записьНабора.ЕдиницаИзмерения = записьНабора.Номенклатура.ЕдиницаИзмерения Тогда
				записьНабора.ЕдиницаИзмерения = записьНабора.Номенклатура.ЕдиницаИзмерения;
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры
